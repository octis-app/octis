// vite.config.local.js — LOCAL OVERRIDES (gitignored)
// Copy this file to vite.config.local.js and customize for your deployment.
// Example: deploying under a subpath via reverse proxy (Caddy, nginx, etc.)
export default {
  base: '/octis/',  // Change to your subpath, e.g. '/dev.octis/' or '/'
}
